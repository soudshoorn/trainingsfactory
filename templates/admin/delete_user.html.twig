{% extends 'base.html.twig' %}

{% block body %}
    <div class="verify">
        <div class="container">
            <div class="row">
                <div class="verify__wrapper">
                    <p class="verify__para">Wilt u {{ user.email }} verwijderen?</p>
                    <div class="verify__buttons">
                        <button class="btn" onclick="location.href='{{ path('admin_index') }}'">Nee</button>
                        <button class="btn" onclick="location.href='{{ path('admin_delete_user', {id: user.id}) }}'">Ja
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}